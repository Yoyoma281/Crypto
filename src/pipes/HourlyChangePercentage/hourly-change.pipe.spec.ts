import { HourlyChangePipe } from './hourly-change.pipe';

describe('HourlyChangePipe', () => {
  it('create an instance', () => {
    const pipe = new HourlyChangePipe();
    expect(pipe).toBeTruthy();
  });
});
